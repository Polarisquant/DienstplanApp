/**
 * @deprecated Statischer Katalog — Feiertage werden per Seed aus `date-holidays` erzeugt
 * (siehe `prisma/holidaysSeed.ts` und `prisma/seed.ts`).
 */
export const FEIERTAGE_SEED: { d: string; name: string; region: string }[] = [];
